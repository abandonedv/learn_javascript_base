// несколько условий
// and
let x = 1;
if (x >= 2 && x <= 7) console.log("x попадает в диапазон [2; 7]");
else console.log("x не попадает в [2; 7]");

// or
if (x < 2 || x > 7) console.log("x не попадает в диапазон [2; 7]");
else console.log("x попадает в [2; 7]");

// and приоритетнее